Startup project for Rails with 1140px grid system

Rails 3.2.0rc1
using MySQL
gem 'rake'
gem 'execjs'
gem 'paperclip', '~> 2.4'
gem 'haml'
group :production do
  gem 'therubyracer'
end
gem 'fancybox-rails'
group :test do
  # Pretty printed test output
  gem 'turn', '~> 0.8.3', :require => false
end
gem 'will_paginate'
